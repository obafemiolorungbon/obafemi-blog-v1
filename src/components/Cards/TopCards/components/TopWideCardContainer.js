import styled from 'styled-components';

export const TopWideCardContainer = styled.div`
  background-color: white;
  width: 80%;
  border-radius: var(--card-border-radius);
  padding: 10px;
  box-shadow: var(--box-shadow);
  overflow: hidden;
`;
